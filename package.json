{
  "name": "bean-bros-brewing",
  "private": true,
  "version": "1.0.0",
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/database": "^2.0.1",
    "stripe": "^17.4.0"
  },
  "scripts": {
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17"
  }
}
