'use strict';
(async()=>{try{
 const response=await fetch('/.netlify/functions/accessories');if(!response.ok)return;
 const {items,banner}=await response.json();if(!Array.isArray(items))return;
 const visible=items.filter(p=>p.active),escape=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const grid=document.querySelector('.mug-launch-grid');
 if(grid){const featured=visible.filter(p=>['travel-mug','speckled-mug'].includes(p.id));const section=grid.closest('section');section.hidden=banner?.visible===false||!featured.length;if(banner){section.querySelector('.eyebrow').textContent=banner.badge;section.querySelector('h2').innerHTML=escape(banner.title)+'<br><em>'+escape(banner.highlight)+'</em>';section.querySelector('h2 + p').textContent=banner.description;section.querySelector('.mug-launch-note').textContent=banner.note;section.querySelector('a.button').textContent=banner.buttonText;}grid.innerHTML=featured.map(p=>`<figure><img src="${escape(p.images[0])}" alt="${escape(p.name)}" loading="lazy" width="1254" height="1254"><figcaption><strong>${escape(p.name)}</strong><span>${escape(p.subtitle)}</span><span>${!p.comingSoon||p.showLaunchBadge===false||banner?.showItemLabels===false?'':escape(p.launchLabel??banner?.itemLabel??'Coming soon')}${p.price==null?'':' · '+new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(p.price)}</span>${p.purchasable?`<a class="button dark" href="/?product=${p.productId}">Shop now</a>`:''}</figcaption></figure>`).join('')}
 if(window.BEAN_PRODUCTS){const list=window.BEAN_PRODUCTS;for(let n=list.length-1;n>=0;n--)if(list[n].category==='accessories')list.splice(n,1);list.unshift(...visible.map(p=>({...p,launchLabel:p.launchLabel??banner?.itemLabel??'Coming soon',showLaunchBadge:p.comingSoon&&p.showLaunchBadge!==false&&banner?.showItemLabels!==false})));render()}
}catch{}})();
