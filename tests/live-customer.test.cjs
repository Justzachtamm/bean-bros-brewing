const {test}=require('node:test'),assert=require('node:assert/strict'),{loader}=require('./helpers.cjs');
test('live checkout replaces a missing test customer reference without replacing the website account',async()=>{let args;const {checkoutCustomer}=loader({'./db':{one:async(s,a)=>{args=a;return{stripe_customer_id:a[1]}}}})('netlify/functions/lib/customer.js');const stripe={customers:{retrieve:async()=>{throw{code:'resource_missing'}},list:async()=>({data:[{id:'cus_live'}]})}};assert.equal(await checkoutCustomer(stripe,{id:1,email:'owner@example.test',emailVerified:true,stripeCustomerId:'cus_test'}),'cus_live');assert.deepEqual(Array.from(args),[1,'cus_live','cus_test']);stripe.customers.retrieve=async()=>{throw{code:'api_connection_error'}};await assert.rejects(()=>checkoutCustomer(stripe,{id:1,emailVerified:true,stripeCustomerId:'cus_test'}));});
