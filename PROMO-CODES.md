# Checkout and promo codes — release handoff

Current release (September 11): production deploy `6aa3870fc4ae7b02fb0acc95`, https://beanbrosbrewingco.com. User authorized live checkout fixes. No git push (avoids duplicate deployment).

Checkout: Apple Pay and Google Pay mount before delivery with deferred Elements. Partial wallet destination gets real UPS rates and Stripe Tax calculation. Full wallet address is verified with UPS before custom Checkout Session creation; final session total must match the wallet-authorized amount. Changed totals or ambiguous addresses stop payment and open prefilled manual checkout. Actual wallet charges remain for the user's live test.

Manual fallback: Stripe Payment Element and shipping Address Element share an Elements group, providing Stripe's Google autocomplete without another API key. Selected address syncs existing validated server inputs. Quote/review debounces automatically; no separate Get rates/Review steps. Optional email choices follow Pay; for express, follow wallet buttons. Paid live signed webhook records explicit opted-in categories in checkout-email-consents, keyed by session ID. No marketing messages sent. Cookie preferences are a bottom banner.

UPS: production account successfully returned a valid street match for public sample 350 Fifth Avenue, New York NY 10118. Validation uses /api/addressvalidation/v2/3, not rating as a substitute. Ambiguous/no match returns 422 with up to three suggested addresses; verification outage blocks checkout with retry. Partial wallet quotes defer street validation until full address is available.

Stripe settings: live domain beanbrosbrewingco.com registered and enabled (pmd_1UELrjKrXp6z7wmBDwtgsopf); Google Pay enabled, Apple Pay already enabled. Chrome live verification shows both Apple Pay and Buy with GPay before any delivery fields. Clover Stripe.js and custom Session API 2025-09-30.clover; existing other server APIs unchanged.

Checks: 117 tests passed, build and diff check passed. Local browser autocomplete selected 350 Fifth Avenue and filled New York, NY, 10118. Cookie bottom banner visually verified. Live valid address returned four UPS rates and an unpaid custom Stripe Session; nonexistent address rejected 422. No payment completed, card entered, or shipment created. User must test wallet/payment completion, including bank authentication/3DS. Keep this limitation explicit.

Local preview on 8765: /private/tmp/bean-checkout-preview-fixed.py serves dist; stale /checkout-preview.html redirects to storefront. Its backend pointer /private/tmp/bean-checkout-draft.json points to the final production deploy URL. No credentials or server files in dist.

Subscription builder release (authorized for publishing): two steps with Next/Back, inline grind and quantity, both Add to cart and Add to subscription under every item, and repeat/multiple adds without leaving the builder. Mushroom choices are exactly Lion’s Mane (catalog Neuroshroom), Reishi Mushroom and Thrive Mode. Those three can recur at full catalog price; coffee retains 10% off. Backend enforces eligibility and cadence. One-time items and recurring items still require separate checkouts; this is disclosed in the builder. 119 tests and build passed. Publish through the main-branch GitHub push; Netlify automatic builds are enabled. The cart also shows Curious about mushroom coffee? after coffee is added, with add buttons matching the cart purchase type. Separate packaging-image edits are excluded from this release.

September 11 follow-up: product-name SQL matching now trims saved names (fixes Fara Estate Nicaragua trailing space) and stock decrement uses the same normalization. Express startup skips remote tax-registration calls, but retains saved tax profiles; final payment still checks registration. Loading failures clear the message and show Retry; wallet Element has a 15s fallback. Commit e8270a8, followed by 4d9fd1c for authenticated Stripe catalog comparison/sync and restored tax profile lookup. Both are local commits pending GitHub authentication (origin/main ends at da2f1a1); direct production deploy 6aa38f8188ac3b3b7c3824fd is live. 123 tests/build passed. Local preview backend pointer updated to this deploy.

All-product live check: 39 catalog records, 33 active purchasable items passed wallet-start at exact catalog price, five inactive/sold-out skipped. Chlorella Boost (900024) still fails unsupported/missing tax category. Do not claim all products pass. Product-name failure is fixed.

Stripe catalog sync completed September 11 after user signed in to Chrome admin tab 1451017608. Initial comparison: 0/39 matched. Ran authorized Sync missing or changed items; fresh comparison confirmed 39/39 store items match Stripe, including names, active status and current one-time USD prices. Existing subscriptions unchanged. No new deploy required for this data sync. Admin auto-refresh can clear the progress status during long sync, but the completed sync was verified with a fresh Check.

Chlorella Boost remains the only active checkout initialization failure. Signed-in Accounting shows Needs classification; Alfalfa Fuel (hidden) is also unclassified. Available categories are coffee, tea, accessories, prepared_food, none clearly applicable to Chlorella powder. Local label collections/assets/herbs/herb-23-back.png contains serving/preparation information but no Nutrition Facts or Supplement Facts panel. Stripe docs distinguish dietary supplements (txcd_40090001, Supplement Facts) from nutritional supplements (txcd_40090008, Nutrition Facts). Need supplier packaging classification from user before choosing/adding the appropriate category; do not substitute tea or disable tax to bypass error. Other existing tax profiles were not audited for legal correctness. Last product QA report /private/tmp/bean-all-product-checks.json. Latest two code commits still pending GitHub authentication; live deploy already contains them.

September 11 nutrition follow-up: user confirmed Nutrition Facts for Chlorella. Added nutritional_supplements txcd_40090008 and admin option, verified against Stripe tax-code documentation. 124 tests and build passed. Commit 1de75f4; production deploy 6aa394fb88ac3b644f3824f8. Signed-in admin saved Chlorella Boost tax profile to nutritional_supplements and refreshed data confirmed selection. Local preview backend pointer updated. No Git push (authentication pending; avoids duplicate production build).

Checkout layout follow-up: manual delivery/card fields now open below express on each checkout opening; removed manual-entry toggle. Email preferences stay below Pay now. 124 tests/build passed. Commit 8c43bc5; live deploy 6aa40940c5217a7038b85241.

Promo typing fix: user could not type because Apple Pay open set checkingOut and locked promo input. Promo now locks only during actual confirmation, while wallet quote must match the entered promo before charge. Added Apply/Enter validation (promo-check action, server-owned cart prices, no address/session required). Initialization revision now separate from payment revision so early promo edits cannot abort wallet startup. 126 tests/build passed. Commits 0e90622,32eeca3,83d4eb2; latest live deploy 6aa40f73f4b5f5ac6e5f23a6. Prior verification caught and corrected a promo-state initialization ordering issue.

Wallet startup performance: Stripe library preloads when a nonempty bag opens; library and authoritative wallet-start request run concurrently. wallet-start includes matching publishable key, removes separate payment-config request, skips shipping settings lookup until quote. Express mounts before manual card/address setup; card init failure no longer suppresses express. 129 tests/build passed; commit 0b7b8b5; live deploy 6aa418f88d9a72d0f48d8cd8. No actual charge or exact speed guarantee.
