# Checkout and promo codes — release handoff

Current layout: one vertical column with express wallets at the top, delivery details, payment details, then promo entry immediately above the checkout/pay control. This order is the same on desktop and mobile. Stripe Card Element loads on opening checkout, before an address or payment session is required. Card details stay with Stripe. Optional separate billing address uses Stripe Address Element. Email is collected for the receipt.

After delivery entry and UPS rate selection, Review order total creates a custom Checkout Session using server catalog prices, validated shipping, optional promo, and automatic tax. The page shows Stripe's final total before the explicit Pay button confirms a payment-method ID. Apple Pay and Google Pay use Stripe Express Checkout buttons after the final total is available, when device/account/domain eligibility allows. Editing delivery invalidates review/payment but preserves the card field; closing checkout clears it. Fulfillment and receipt verification still use Checkout webhooks. Existing hosted and embedded callers remain supported. Custom session creation alone pins API version 2025-09-30.clover.

Promo codes: Admin → Promo codes supports percent or USD discounts, expiration, redemption limits, listing and deactivation. One-time purchases only, excluding shipping; subscriptions retain existing 10% savings. Free-shipping eligibility uses pre-promo subtotal.

Setup: supplied live STRIPE_PUBLISHABLE_KEY saved in Netlify function scopes for production and deploy-preview. Stripe Card Element successfully loaded in the actual local checkout. Domain registration/wallet eligibility and completed payments remain unverified. No secret key is exposed to the browser.

Validation on September 11: user explicitly approved saving locally verified UPS credentials to Netlify deploy-preview. All three UPS settings were saved successfully. Latest draft: https://6aa37d556615198f7b16ed7d--beanbrosbrewingco.netlify.app. No production release. 100 tests pass, build passes, diff check passes.

Real browser verification on local preview using the draft backend: live UPS rates returned (Ground $7.77 for sample address 350 Fifth Avenue, New York 10118); custom live Stripe Session created successfully; order review displayed $19.99 items + $7.77 shipping + $0.00 Stripe-calculated tax = $27.76. Blank card rejected as incomplete. Invalid promo rejected. Editing delivery/promo cleared the payable review and allowed retry. No card details entered, charge attempted with a usable payment method, or payment completed. Express Checkout Element mounted but this browser offered no eligible wallet. Successful payment, declines, 3DS, wallet payment, valid promo redemption and fulfillment still require a test-mode end-to-end check.

Compatibility fix from real testing: Stripe.js v3 cannot initialize custom Checkout; Basil has the older fetchClientSecret API. Frontend now loads https://js.stripe.com/clover/stripe.js, matching the custom session creation API 2025-09-30.clover. Other server-side Stripe calls retain their existing version.

Subscription builder: Build my subscription opens an in-stock coffee chooser. Selection opens grind, quantity and cadence with subscription selected; existing cadence is preserved. In-stock mushroom suggestions link to product details and are marked separate one-time purchases. Browser verified chooser, discounted price and add-to-bag. Included in latest draft.

Local preview: /private/tmp/bean-checkout-preview-fixed.py serves dist on port 8765 and redirects the obsolete /checkout-preview.html path to the storefront, surviving rebuilds. /private/tmp/bean-checkout-draft.json selects the latest draft backend for local QA; return URLs are rewritten to the allowed production origin for unpaid session testing. Local server is a QA tool, not production code. Production remains unchanged.

Production release September 11: user explicitly requested publishing for their own live testing. Deployed once with production context, deploy 6aa37e0f30b330e7c6b04018, https://beanbrosbrewingco.com. Live verification passed: subscription chooser present, Clover library served, live publishable key configured, UPS returned four services, custom live Checkout Session created successfully. No payment confirmed. Earlier statements that production is unchanged are superseded by this release. User will perform their own live payment test; wallet/payment completion remains unverified by us.
