# Checkout and promo codes — release handoff

Current release (September 11): production deploy `6aa3870fc4ae7b02fb0acc95`, https://beanbrosbrewingco.com. User authorized live checkout fixes. No git push (avoids duplicate deployment).

Checkout: Apple Pay and Google Pay mount before delivery with deferred Elements. Partial wallet destination gets real UPS rates and Stripe Tax calculation. Full wallet address is verified with UPS before custom Checkout Session creation; final session total must match the wallet-authorized amount. Changed totals or ambiguous addresses stop payment and open prefilled manual checkout. Actual wallet charges remain for the user's live test.

Manual fallback: Stripe Payment Element and shipping Address Element share an Elements group, providing Stripe's Google autocomplete without another API key. Selected address syncs existing validated server inputs. Quote/review debounces automatically; no separate Get rates/Review steps. Optional email choices follow Pay; for express, follow wallet buttons. Paid live signed webhook records explicit opted-in categories in checkout-email-consents, keyed by session ID. No marketing messages sent. Cookie preferences are a bottom banner.

UPS: production account successfully returned a valid street match for public sample 350 Fifth Avenue, New York NY 10118. Validation uses /api/addressvalidation/v2/3, not rating as a substitute. Ambiguous/no match returns 422 with up to three suggested addresses; verification outage blocks checkout with retry. Partial wallet quotes defer street validation until full address is available.

Stripe settings: live domain beanbrosbrewingco.com registered and enabled (pmd_1UELrjKrXp6z7wmBDwtgsopf); Google Pay enabled, Apple Pay already enabled. Chrome live verification shows both Apple Pay and Buy with GPay before any delivery fields. Clover Stripe.js and custom Session API 2025-09-30.clover; existing other server APIs unchanged.

Checks: 117 tests passed, build and diff check passed. Local browser autocomplete selected 350 Fifth Avenue and filled New York, NY, 10118. Cookie bottom banner visually verified. Live valid address returned four UPS rates and an unpaid custom Stripe Session; nonexistent address rejected 422. No payment completed, card entered, or shipment created. User must test wallet/payment completion, including bank authentication/3DS. Keep this limitation explicit.

Local preview on 8765: /private/tmp/bean-checkout-preview-fixed.py serves dist; stale /checkout-preview.html redirects to storefront. Its backend pointer /private/tmp/bean-checkout-draft.json points to the final production deploy URL. No credentials or server files in dist.

Subscription builder release (authorized for publishing): two steps with Next/Back, inline grind and quantity, both Add to cart and Add to subscription under every item, and repeat/multiple adds without leaving the builder. Mushroom choices are exactly Lion’s Mane (catalog Neuroshroom), Reishi Mushroom and Thrive Mode. Those three can recur at full catalog price; coffee retains 10% off. Backend enforces eligibility and cadence. One-time items and recurring items still require separate checkouts; this is disclosed in the builder. 119 tests and build passed. Publish through the main-branch GitHub push; Netlify automatic builds are enabled. The cart also shows Curious about mushroom coffee? after coffee is added, with add buttons matching the cart purchase type. Separate packaging-image edits are excluded from this release.
